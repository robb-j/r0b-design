{{#each variables}}
<div class="variable-group">
  <h3 class="skeleton-title">{{@key}}</h3>
  {{#if colors }}
  {{> color-list colors=colors}}
  {{/if}}

  {{#if fonts }}
  {{> font-list fonts=fonts message=@data.longTitle}}
  {{/if}}
</div>
{{/each}}
